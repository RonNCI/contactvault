<%= render 'shared/navbar' %>
<!-- container for new contact form -->
<div class="container mt-4">
  <!-- page heading -->
  <h2>Add New Contact to Vault</h2>
  <!-- render the contact form partial -->
  <%= render 'form', contact: @contact %>
</div>